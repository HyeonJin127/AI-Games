*AI 폴더안에서 실행*
### python -m venv .venv <- .venv 가상환경 생성
### .venv\Scripts\activate
### pip install -r requirements.txt 

### .venv\Scripts\activate <- 가상환경 실행

-------------------------

1. 학습 데이터 생성 위치

train_qlearning.py를 실행하면, 학습이 끝난 후
다음 파일들이 프로젝트 폴더 (mazes/) 에 생성된다.

mazes/
├─ qtable.pkl
├─ rewards.csv

---------------------------

2. qtable.pkl (강화학습 핵심 데이터)
✅ 역할

AI1(Q-learning 에이전트)의 학습 결과(정책, policy) 를 저장한 파일

난이도 선택 및 모델 로딩과 직접적으로 연관되는 핵심 데이터

✅ 파일 정보

위치: mazes/qtable.pkl

형식: Python dict를 pickle로 직렬화한 바이너리 파일

---------------------------

3. Q-table 내부 구조

train_qlearning.py에서 Q-table은 다음과 같이 선언됨:

Q = defaultdict(float)

🔹 Key / Value 구조

Key: (state, action)

state : (x, y) → AI1의 현재 미로 좌표

action: int → 이동 방향

0: UP

1: DOWN

2: LEFT

3: RIGHT

Value: float

해당 상태에서 해당 행동을 선택했을 때의 Q-value

🔹 개념적 테이블 형태
x	y	action	q_value
1	1	0	0.13
1	1	1	-0.05
2	1	2	1.27
…	…	…	…
🔹 실제 논리 구조
Q[((x, y), action)] = q_value


학습이 끝나면 다음 코드로 파일에 저장됨:

plain_q = dict(Q)
pickle.dump(plain_q, f)   # qtable.pkl

---------------------------

4. rewards.csv (학습 성능 로그)
✅ 역할

에피소드별 총 리워드 기록

학습 성능 분석 및 그래프 시각화 용도

✅ 구조 예시
episode,reward
1,-32.4
2,-21.5
3,-18.0
...
300,8.0


⚠ 난이도 선택에는 직접적으로 사용되지 않음
(분석/리포트/시각화 목적)

---------------------------

5. 난이도 선택 & DB 저장 설계 가이드

현재는 파일 기반(pickle) 저장이지만,
난이도 선택 기능을 위해 DB로 확장할 수 있도록 구조가 단순하게 설계됨.

✅ DB 테이블 설계 예시
컬럼명	설명
difficulty	난이도 (easy / normal / hard)
x	AI1 위치 x
y	AI1 위치 y
action	행동 (0~3)
q_value	Q-value
✅ 난이도별 정책 관리 방식

난이도별로 다른 Q-table(정책) 사용 가능

EASY → Q_easy

NORMAL → Q_normal

HARD → Q_hard

✅ 게임 실행 흐름 (나중 단계)

플레이어가 난이도 선택

DB에서 해당 난이도의 Q-table 로드

(x, y, action) → q_value 형태로 재구성

main.py에서 Q-table 기반 정책으로 AI1 실행

---------------------------

# 한줄정리

학습 결과는 qtable.pkl 파일에 저장되며,
(x, y, action) → q_value 형태의 Q-table(dict)로 구성된
강화학습(Q-learning) 정책 데이터이다.
